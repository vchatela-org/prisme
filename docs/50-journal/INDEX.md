# Journal

Append-only run notes. Every agent or person finishing a piece of work adds an entry and a row here.

This is the project's **memory**: it survives sessions, and it is what a later agent reads to avoid
re-deriving something that has already been worked out — or re-making a mistake that has already
been made once.

## ⚠ The journal is public

Entries record **what was decided and why, never what the data said.**

- ✅ "Adopted 7 unlinked objectives; 2 needed manual matching because titles had diverged."
- ❌ Anything naming a real objective, project, area, task or workspace.

Debugging notes that genuinely need real values go in a `<entry>-private.md` sibling, which is
gitignored. This is the likeliest leak path in the project, because it happens under pressure —
see [`../17-privacy.md`](../17-privacy.md).

## Entries

| Date | Workstream | Entry | Summary |
|---|---|---|---|
| 2026-09-15 | P0 | [P0-2026-09-15-foundation-docs.md](P0-2026-09-15-foundation-docs.md) | Model, ADRs, workstream briefs and privacy machinery written |
| 2026-09-15 | P0 | [P0-2026-09-15-oq9-identity.md](P0-2026-09-15-oq9-identity.md) | OQ-9 closed: forward-auth with a **verified** assertion (ADR-0021). W14 unblocked |
| 2026-09-15 | P0 | [P0-2026-09-15-backups-are-infrastructure.md](P0-2026-09-15-backups-are-infrastructure.md) | Backups owned by the deployment repository as a dump CronJob (ADR-0022); this repo ships none. Gates step 8 only |
| 2026-09-15 | P0 | [P0-2026-09-15-publication-sweep.md](P0-2026-09-15-publication-sweep.md) | Sweep run, history redacted, **repository made public**. MIT chosen (OQ-8). CodeQL still owes `javascript-typescript` |
| 2026-09-15 | P0 | [P0-2026-09-15-model-frozen.md](P0-2026-09-15-model-frozen.md) | **P0 frozen.** Dual-owner cell resolved to ADR-0008; cadence map written; 14 anchors and the OQ numbering fixed. Wave 1 cleared |
| 2026-09-15 | P0 | [P0-2026-09-15-branch-and-pr-protocol.md](P0-2026-09-15-branch-and-pr-protocol.md) | Every workstream lands as a **PR on its own branch**, green before it stops, human merges. `enforce admins` closes the bypass that let agents push to `main` |
| 2026-09-15 | P0 | [P0-2026-09-15-ci-verification.md](P0-2026-09-15-ci-verification.md) | "Green" now means **read back from the pull request** after the last push — queued, in progress or absent is not green. Written into all five layers of the branch-and-PR protocol |
| 2026-09-15 | W00 | [W00-2026-09-15-foundations.md](W00-2026-09-15-foundations.md) | Monorepo, per-service config loader, standalone migration runner, redacting logger, distroless images, and the CI gates — both security gates **watched fail** before being trusted. CodeQL now covers `javascript-typescript` |
| 2026-09-15 | W00 | [W00-2026-09-15-close-out.md](W00-2026-09-15-close-out.md) | Post-merge: W00 landed (#5), Harbor configured, a no-reader variable removed, merged branches deleted. First tag `v0.0.1` run — publish failed on a cluster runner with **no Docker socket**, fixed with the `kubernetes` buildx driver |
| 2026-09-15 | P0 | [P0-2026-09-15-node-26-baseline.md](P0-2026-09-15-node-26-baseline.md) | Node 26 becomes the toolchain baseline (ADR-0023); corepack is gone from Node, so the builder installs pnpm from npm at the version `packageManager` pins. Web builder and `engines` follow in PR #10 |
| 2026-09-16 | P0 | [P0-2026-09-16-node-26-web-and-engines.md](P0-2026-09-16-node-26-web-and-engines.md) | Second half of ADR-0023: web builder and its distroless runtime on Node 26 with the same derived pnpm bootstrap, then `engines.node` to `>=26.0.0` — the move `engine-strict` forced to go last. Toolchain now consistent end to end |
| 2026-09-16 | W01 | [W01-2026-09-16-domain-scoring.md](W01-2026-09-16-domain-scoring.md) | Entities, scoring registry, `wsjf-balanced` v1, capacity and now-set selection, plus the domain tables. Properties tested **exhaustively** over the closed input space rather than sampled. The worked example reproduces end to end; purity lint, the new golden/version gate and every database guard were **watched fail** |
| 2026-09-16 | W03 | [W03-2026-09-16-connectors.md](W03-2026-09-16-connectors.md) | Read path to both external tools: role-keyed queries, the two-minute watermark overlap with content hashing, allow-listed sanitisation, the retry policy, and recorded-fixture clients. The issue path is **redacted** before it reaches an error — a document tool keys its properties by names that are instance data |
| 2026-09-16 | W02 | [W02-2026-09-16-schedule-engine.md](W02-2026-09-16-schedule-engine.md) | CPM forward and backward passes, per-area capacity as a concurrency limit floored at one slot, deadline feasibility that flags and never writes, and a replan that recomputes rather than patches. Checked against **hand-computed** networks. Working days are integer UTC day numbers, not a date library — and slack deliberately does not know about capacity |
| 2026-09-16 | W07 | [W07-2026-09-16-design-system.md](W07-2026-09-16-design-system.md) | Tokens, primitives, domain components, chart wrappers, shell and the gallery. The `dataviz` palette adopted and **re-validated** in both modes; an in-fill label helper written, measured at 4.46:1 and deleted. Two bugs no static check could see — a client/server boundary that only fails on request, and a modifier key the component never passed — found by **running** the gallery |
| 2026-09-17 | W04 | [W04-2026-09-17-reconciler.md](W04-2026-09-17-reconciler.md) | The level-triggered reconciler: pure planner, plan/apply, the no-duplicate guards, the conflict matrix, the intent channel and the write path. The adversarial test is **exhaustive** over the closed input space, and convergence is proved by re-planning against an applied world. Two bugs only running found — Drizzle swapping the driver's date handling on the client it wraps, and a column exactly as wide as its widest value |
| 2026-09-17 | W05 | [W05-2026-09-17-api.md](W05-2026-09-17-api.md) | The REST API: 53 routes, each declaring its scope, with the OpenAPI document generated from the same objects the router mounts. The deny-by-default test reads back what Hono **registered** rather than what the registry claims. Two bugs only a real database could show — `delete` refused by an append-only trigger, and `jsonb` arriving as text on which every property access silently yielded `undefined` |
| 2026-09-18 | W14 | [W14-2026-09-18-security.md](W14-2026-09-18-security.md) | Authentication and authorization: the verified assertion, Argon2id scoped tokens, deny-by-default installed, diff-bound confirmations, a kill switch that withholds scopes rather than setting a flag, and a strict CSP with per-request nonces. Eight things only running found — a `READ COMMITTED` race that gave a single-use token two winners, middleware silently not loaded so every security header was absent, and a dependency edge that would have shipped a database driver to the tier that must not hold one. Both secret-scanning gates now have a **negative-control job** rather than a memory of having watched them fail |
| 2026-09-18 | W06 | [W06-2026-09-18-mcp.md](W06-2026-09-18-mcp.md) | Seventeen MCP tools on one stateless endpoint, the protocol written against its specification rather than the SDK (ADR-0024) — seventeen dependencies, including a second HTTP framework and an OAuth client, avoided at the cost of owning conformance. Every write tool is a dry run whose confirmation is bound to a diff **re-derived at execution**. Three things only running found: a `create` diff that could never go stale, a state probe inflating the one count ADR-0010 is about, and a generated manifest carrying literal UUIDs the deny-list was right to refuse. Tool descriptions reviewed by a cold agent, twice — the first run could not complete four of twelve requests |
| 2026-09-18 | W14 | [W14-2026-09-18-settings-followup.md](W14-2026-09-18-settings-followup.md) | The two settings W14 left for a human, closed: `main` now requires `golden fixtures` and `security gate self-test`; secret scanning's validity checks and non-provider patterns are on via a repo-scoped org configuration, after the org default's attach failed silently on a CodeQL setup collision |
| 2026-09-19 | W08 | [W08-2026-09-19-ui-focus.md](W08-2026-09-19-ui-focus.md) | Focus, Backlog, Inbox and initiative detail, with every decision the screens make extracted into pure, tested functions. Three things only running found: `.js`-suffixed relative imports that `tsc` accepts and the Next bundler does not, configuration read at module scope making `next build` demand production secrets to emit a manifest, and **W14's CSP refusing every server-rendered `style` attribute** — a nonce cannot authorise one, so `packages/ui` area colour is absent until hydration re-applies it through the CSSOM, and each page logs ~100 violations. Not W08's to fix, and not fixable by relaxing the policy. `pnpm lint` is OOM-killed as a single process on a small machine and is green per package; all 17 CI checks green |
| 2026-09-19 | W07 | [W07-2026-09-19-csp-inline-style.md](W07-2026-09-19-csp-inline-style.md) | The defect W08 found, closed: `packages/ui` and `apps/web` no longer emit a `style` attribute at all. Colour reaches HTML as a literal utility class and SVG as a `fill` attribute, both derived from one slot so a bar and its tooltip cannot disagree; the balance meter's fill is an SVG `rect`, because a length from data is exact as an attribute and would have to be rounded as a class. Guarded by the repository's first component test — rendered markup asserted to carry no `style=` — plus a source walk over both trees. Driven in a browser: ~100 violations on the gallery became 12, all of them Radix's own markup, which is recorded rather than solved |
| 2026-09-19 | W12 | [W12-2026-09-19-adoption.md](W12-2026-09-19-adoption.md) | The adoption path: a scan that mirrors rather than accumulates, a queue that holds only what would become an entity, and three decisions none of which writes outward. Rule 4 needed a **word-level** test on top of its threshold — pure Dice scores two different annual reviews at 0.90, and no threshold separates that pair without rejecting every real near-match. The adversarial test runs the real planner over an adopted corpus in every status and link state, with a control that flips origin alone and gets a create. One dependency found missing: nothing in the repository loads the document tool's role bindings, so the scan runs on the task tool alone and says so |
| 2026-09-19 | W09 | [W09-2026-09-19-ui-areas-kpi.md](W09-2026-09-19-ui-areas-kpi.md) | Areas, area detail, Year Review and the KPI dashboard, with the year-boundary rule written first and proved live: a 2027 decision leaves 2026's target line where it was. Five things only running found, four of them nobody else's to hit again — **every write from `apps/web` was answered 403** because the API's CSRF check requires an `Origin` this tier never sent (W08's and W12's writes as much as this one's), and **`packages/ui`'s charts could not be rendered by a server component at all**, because `format` is a function prop. Both fixed. Cycle time is deliberately not drawn and the dashboard says why. Area colour still collides on every screen: the pinning map exists and nothing mounts it |

| 2026-09-20 | W11 | [W11-2026-09-20-ui-objectives-reviews.md](W11-2026-09-20-ui-objectives-reviews.md) | Objectives, key results and the four cadence reviews. The wizard encodes the cadence map's step *shapes*, never a checklist, which is instance data; resume is the **first unticked** step, and every tick is a read-merge-write because `PATCH /reviews/:id` replaces the checklist wholesale — sending only the known steps would delete an instance's own. Two shared defects found: **`areaListSchema` has never parsed a real `/areas` response**, so five other screens had been showing the area *key* where they meant the name since W08, and **`fixtures/objectives.json` was loaded by nothing** and had drifted to three statuses the schema forbids. The detail panel claimed "nothing computes progress" above a computed 70%, because computed progress comes from the serving initiatives and not from the key result's own anchor. The harness was built and thrown away for the **fifth** time, with two notes added: the SSRF guard refuses a loopback JWKS URL, and both tiers must share `PRISME_BASE_URL` or every write is 403 |

| 2026-09-20 | W13 | [W13-2026-09-20-backfill.md](W13-2026-09-20-backfill.md) | Completion history → per-area capacity actuals, ritual adherence and materialised weeks. **The location is stored and the area is not**, so adding one `area_mapping` row re-attributed a whole corpus with zero windows fetched. Idempotence is `(task, completed_at)` — a property of the schema, not of the code writing to it. A day-scale duration is a block-out, not an effort, and is counted rather than converted. One defect only a real second run found: the materialised weeks and the covered instants are **not the same range**, so a backfill starting on a Sunday collided on `capacity_week`'s key — and the in-memory store had the identical off-by-one, so the unit suite agreed with the SQL and both were wrong. A new table with a foreign key turned 106 tests red in two suites nobody had touched, which is the no-`cascade` rule working. The document tool is still not read, so the declared-duration tier is unavailable — W12's gap, not a second one |

| 2026-09-19 | W10 | [W10-2026-09-19-ui-timeline.md](W10-2026-09-19-ui-timeline.md) | The Timeline: bars, dependency edges, the critical path, deadline markers and drag-to-replan, with nothing on the screen computing a date. **The API had no replan endpoint** — W02 built `replan`, W05 never exposed it — so one was added as a `GET` on a read scope, because a preview behind a write scope goes dark exactly when the kill switch is pulled. A drag writes `earliest_start` and there is nowhere for it to write anything else. Five defects only running found, including a bar whose hit area was the width of the plot and the left-edge mirror of the axis-clipping bug W09 fixed on the right. The harness was built and thrown away for the **fourth** time |

| 2026-09-20 | W15 | [W15-2026-09-20-creation-flows.md](W15-2026-09-20-creation-flows.md) | Quick capture, new initiative and new project, plus the ledger that makes a multi-tool creation survivable: there is no transaction spanning two SaaS APIs, so the **intention is committed before anything outward is attempted** and a partial creation is a row saying what is missing rather than an orphan. The idempotency key is stored rather than derived per pass — the opposite of the reconciler's rule — because a creation outlives a pass. Promotion **satisfies ADR-0010 guard 2 rather than re-implementing it**: the initiative is inserted with its anchor already set, proved by running the real planner with a control. Six defects only running found, including a route group that served every screen at the wrong URL, `sql.json()` failing on the Drizzle-wrapped client the API actually uses — invisible to an integration suite that builds a bare one — and a frozen deployment recording every intent as failed. One cross-workstream defect: **`GET /areas?limit=200` answers 400**, so five screens have taken their area list's failure path on every load since W08; W11 recorded it as harmless. ADR-0025 raised as Proposed: ADR-0011's *Create page* has no role key naming where a page would go |
| 2026-09-21 | FUP | [FUP-2026-09-21-integration-client.md](FUP-2026-09-21-integration-client.md) | The integration suite now builds the client the application builds (W15's follow-up). Switching `openTestDatabase` to `createDatabase` exposed **three more instances of the same class**, two of them live production defects: `GET /events` answered `500` on a jsonb **string** (`JSON.parse('done')`), and `prisme-sync backfill` failed outright on its `date[]`/`timestamptz[]` window writes because Drizzle replaces those serializers with the identity function. `jsonb` is now read as `::text` and parsed in one place; date arrays cross as `text[]` and cast in SQL. A source-walk guard over all three applications refuses a bare client construction, with one spelled-out escape for the schema-owner connection — **watched fail** before it was trusted |
| 2026-09-21 | FUP | [FUP-2026-09-21-fixture-task-mirror.md](FUP-2026-09-21-fixture-task-mirror.md) | `fixtures/task-mirror.json` gives the fixture set a task mirror, so `progressComputed` (ADR-0013) and its self-versus-computed divergence are reachable from a plain seed instead of only by hand-seeding rows in a test (W11's finding). Twenty-seven synthetic rows over six cases, including the two `null`s that must not be collapsed. `progressComputedHint` — a field nothing read, now a second source of truth for the same number — is removed. **It widens what every suite sees**, so two suites that measure a window they build themselves now start from an empty mirror, in the same commit rather than left to fail later |
| 2026-09-21 | FUP | [FUP-2026-09-21-area-colour-pinning.md](FUP-2026-09-21-area-colour-pinning.md) | Area colour stops colliding on every screen: `AREA_COLOR_PINS` gives an instance a key→slot pinning map and the **root layout mounts `AreaColorProvider`** with it — one mount, which is what W09 found missing. **Verified by running**: the control reproduces the defect exactly (health and money both slot 8, home and community both slot 7), the pinned run separates them, a degenerate map paints all six one slot, and `/gallery` still shows its own fixture map. A slot outside 1–8 stops the boot naming the key |
| 2026-09-21 | FUP | [FUP-2026-09-21-capacity-week-reader.md](FUP-2026-09-21-capacity-week-reader.md) | The materialised weeks reach the dashboard: `/balance` and `/kpi` measure from `capacity_week` when the backfill has covered the window and fall back to the anchor subtree when it has not, reporting `observedSource` and `observedThrough` either way (W13's follow-up). `computeCapacityFrom` in `packages/domain` carries the balance rule for totals somebody else measured, and `computeCapacity` now delegates to it so the two share **one** implementation — asserted by comparing outputs rather than two sets of expected numbers. `/kpi` gains its **first integration coverage**; a sentence on both screens says which record the reading came from |
| 2026-09-21 | FUP | [FUP-2026-09-21-role-bindings.md](FUP-2026-09-21-role-bindings.md) | **The document tool is finally readable**: the role bindings load from the seed path into a `role_binding` table and `adopt` and `backfill` now build a `DocToolClient` from them, closing the gap W12 and W13 both recorded and gating W15's *Create page*. Adds `prisme-sync bindings --from <path>`, `DOCTOOL_DURATION_PROPERTY` for the declared-duration tier, and `fixtures/bindings.json`. A role with no binding is not addressable, which every caller already handles as "not read" — the strongest evidence the connectors were written for this and only the loader was missing |




| 2026-09-21 | FUP | [FUP-2026-09-21-page-creation.md](FUP-2026-09-21-page-creation.md) | **ADR-0025 Accepted and *Create page* works**: four role keys, a `create` capability narrower than `write` (and deliberately not readable), `assertCreatable`, a writing method on the document-tool client, a second creating port with a frozen twin, and a converge pass that plans and performs pages. The body is a copy of the template's top-level blocks and nothing of prisme's own — so the page intent's draft is now a title and nothing else. Idempotence is the existence check the tool's missing idempotency key forces: a page with this title under this parent is the page this call was going to make. **A capture's page stays blocked**, named as a gap rather than guessed at |

| 2026-09-21 | FUP | [FUP-2026-09-21-radix-inline-styles.md](FUP-2026-09-21-radix-inline-styles.md) | The last twelve CSP violations, closed: **five `pnpm patch`es** move Radix's static inline styles onto class names and the token sheet generates the classes they name. **Verified by running** — `/gallery` goes from 12 `style` attributes to **0**, `/` `/areas` and `/kpi` from 1 to 0, with all five repair classes present in the markup where the inline styles used to be. The guard renders `Select`, `FibonacciSelect`, `Tabs` and `ToastProvider` so a patch invalidated by a dependency bump is a **named failing test** rather than a silent regression; it was watched fail |

| 2026-09-21 | FUP | [FUP-2026-09-21-publish-verified.md](FUP-2026-09-21-publish-verified.md) | **The publish path has been run, and it works** — the one gate W00 failed to watch. Both images built, attested and pushed from the cluster runner, digests read back rather than inferred. Publishes the `:main` tag, which sits awkwardly beside `publish.yml`'s own "a deliberate, named version" rule and is recorded rather than hidden. **A repository variable is not masked in a workflow log, and this repository is public** — a successful publish writes the registry host into a public log, which the previously-failing run never reached |
| 2026-09-21 | FUP | [FUP-2026-09-21-tool-base-urls.md](FUP-2026-09-21-tool-base-urls.md) | `DOCTOOL_BASE_URL` / `TASKTOOL_BASE_URL` land in `@prisme/config`, so the outward path can be pointed at a self-hosted instance, a proxy or a stub instead of a constant being edited by hand (W15's recorded follow-up). Wired into all eight constructors that build an outward URL, **reader and writer both** — a deployment that redirected one but not the other would read from one instance and write to another, and both passes would report success. **No default in the schema**, deliberately: the vendor hostname lives in `packages/connectors`, and a second copy here would be a second place to change. Two stale `backfill/` headers corrected, both still describing the document tool as unread after the bindings pull request made that untrue. One finding worth carrying: **the suggestion that `DOCTOOL_BASE_URL` would unblock *Open page* is wrong** — it is the API host, and the tool's own page URL is deliberately not read, because it identifies the workspace |
| 2026-09-22 | FUP | [FUP-2026-09-22-sync-metrics.md](FUP-2026-09-22-sync-metrics.md) | **Two of the seven metrics `docs/15-runtime.md` §5 specifies were invisible in a deployment**, including the one the spec calls *"the one that actually matters"*. Not a missing `set()` — topology: the reconciler is a CronJob pod with no Service, so Prometheus never scrapes the process that knows the answer, and the API exported both gauges as a constant `0` because **prom-client renders a registered-but-never-set `Gauge` as a sample at zero**. So the staleness alert fired permanently and the drift alert could not fire at all, which looks exactly like coverage. Fixed by writing the pass's outcome to a singleton row (ADR-0018 — all state is already in PostgreSQL) and republishing it on `/metrics`; **absent is not zero**, so an unmeasured gauge is `remove()`d rather than set to `0`. The write lives in `run.ts` rather than either entrypoint, so the scheduled pass and the force-sync button cannot publish different histories. Found by deploying prisme and trying to alert on it — every check in this repository passed while it was broken |
| 2026-09-22 | FUP | [FUP-2026-09-22-todoist-api-v1.md](FUP-2026-09-22-todoist-api-v1.md) | **The task tool's v9 API was removed and every call returned `410 Gone`** — the whole task-tool path, read, completions and write. Found while preparing the deployment; nothing in the repository could have caught it, because no test may call a real API. All three call sites move to the `v1` endpoints the deprecation notice names: `POST /api/v1/sync` keeps v9's form body and response shape for **both** the read and the write, while completion history becomes a `GET` with query-string parameters. Three findings that are not about a URL. **Paging is by cursor, and it ends on the absence of one — never on a short page**; the endpoint may return fewer items than `limit` and still hold more, so the old offset loop silently truncated the history capacity actuals are built from, and the fixture's first page is now short by construction. **The completion item is a task, not a completion record**: the id arrives as `id` where v9 sent `task_id`. **The window is closed at both ends**, so adjacent slices both return a completion sitting on the instant between them — W13's `(task, completed_at)` dedupe is what absorbs it, and that is now load-bearing rather than belt-and-braces. v1 ids are 16-character alphanumeric and v9's numeric ones are rejected with `557 V1_ID_CANNOT_BE_USED`, which makes a write against a pre-migration id fail loudly until a full pass has refreshed it. The migration also **quietly weakened the privacy deny-list**, which matched Todoist numeric ids in three named fields — corrected, with `docs/17-privacy.md` §2.4, rather than left as a follow-up: a control that stops covering what it exists for is a false assurance |

| 2026-09-22 | FUP | [FUP-2026-09-22-doc-tool-api-version.md](FUP-2026-09-22-doc-tool-api-version.md) | **The document-tool client pinned an API version from the era of `/v1/databases/` while calling `/v1/data_sources/`, so every query returned `400 invalid_request_url`** — the whole read path, adoption scan included. The pin's reasoning was never wrong, only its value: `2022-06-28` moved to `2026-03-11`, the latest release, which the vendor's own version list confirms and the live API answers `200` to (as it does `2025-09-03`; an invented date is rejected outright, so guessing forward was never available). Every endpoint was then checked against the new version rather than assumed, and **one shape had moved silently**: `archived` is dropped at this version in favour of `in_trash`, and because both fields are optional in the wire schema nothing failed to parse — a page that is not live would simply have read as live. The mapping already `or`ed the two, so that is now a named function with the reason on it, the fixtures carry one spelling each instead of both at once, and a new guard asserts the pin is at or after the version the surface it calls exists in. The other two breaking changes in that release are checked and do not touch prisme. **There is no second failure in the read path** — filter, sorts, cursor paging, permissions and block recursion all worked unchanged; the second failure is that a refused read prints as "not read", so nothing here would ever have said so |
| 2026-09-22 | FUP | [FUP-2026-09-22-proxy-cannot-sign.md](FUP-2026-09-22-proxy-cannot-sign.md) | **ADR-0021's first deployment obligation is undeliverable**, and the reason is the identity provider's design rather than a misconfiguration: a proxy provider's signing key is nulled by a startup reconcile that a server-side worker re-fork re-runs on a traffic-driven cadence, so an assigned key works and then silently decays — no error, no log line, and the first symptom is a crashlooping pod two layers away. Three earlier explanations (a pod restart, an unlogged write, a six-hour schedule) were each true-sounding and each wrong; "six hours" was how often anyone looked. **A scheduled re-assignment was refused as the answer** — a control that must never fail is worse than a mechanism that does not need one. [ADR-0026](../20-decisions/0026-human-auth-via-oidc.md) proposes the supersession and is **Proposed, not Accepted**; the verification property is kept and only the login location moves, at the honest cost of a session prisme did not previously hold |

| 2026-09-22 | FUP | [FUP-2026-09-22-oidc-human-auth.md](FUP-2026-09-22-oidc-human-auth.md) | **ADR-0026 is Accepted and implemented: humans log in over OIDC, in-app.** The web tier stops inheriting an assertion from a proxy and runs the authorization-code flow itself, then verifies the ID token it gets back with the verifier that was already there — one verifier, unchanged, and the assertion header is no longer read on the way in at all. **PKCE held the no-secret property** (`S256`, a public client, no `OIDC_CLIENT_SECRET` in the schema and a test asserting none appears), and the session it costs — the one ADR-0021 avoided — is the provider's token in an `HttpOnly` `Secure` `SameSite=Lax` `__Host-` cookie with **no server-side store**, bounded by the token's own `exp`, fixated by nobody because no identifier is ever issued or adopted. Verified by running, not by its parts: a real browser and a scripted client drove login → provider → callback → session → a populated screen → a write, and the run exposed six things — a percent-encoded cookie that needs decoding on the way in, two runtime CSP violations from a Radix `Select` popper (pre-existing, recorded as a follow-up), a `307`-by-default redirect, a harness whose seed was not idempotent, the API failing closed and naming the issuer when the key set is unreachable, and `next start` warning under `output: standalone`. The checks that matter are three refusals: a **valid** token in the assertion header authenticates nobody, and a tampered verifier, a mismatched `state` or a replayed code each answer 401 without writing a session |
| 2026-09-23 | FUP | [FUP-2026-09-23-oidc-in-the-cluster.md](FUP-2026-09-23-oidc-in-the-cluster.md) | **The OIDC client exists, in the cluster** — the deployment side of ADR-0026, and the two things that made it less mechanical than "register a client". The client had to be **public** (PKCE, no secret), and the registration tooling could only write confidential ones — registering the wrong shape is silent, because a public client's secret check is skipped rather than failed. And the provider had to be **deleted, not updated**: it was a proxy, a proxy *is* an OAuth2 provider underneath sharing one row, and the script matches by name — so aiming it at that row would have left a proxy and silently kept the key that is assigned and then wiped. The cost is a new client id, so the audience changes on both tiers; the **subject allow-list does not**, and that was checked rather than assumed. Verified against the live provider rather than against the registration's own output: the authorize endpoint accepts the exact callback with an `S256` challenge and refuses a near-miss, and the key set went from empty to one asymmetric key. Running it exposed what a plan would not — Vault reaches a pod on a refresh interval, so the image that requires the new values was about to roll before the Secret had them, which is a boot refusal whose only clue is a variable name. The apply merged and applied, and both tiers now run the version that logs humans in |
| 2026-09-23 | FUP | [FUP-2026-09-23-oidc-signin-completes.md](FUP-2026-09-23-oidc-signin-completes.md) | **The human login completes, in the cluster** — the boundary the preceding entry stopped at on purpose, because crossing it needs a real account and an agent cannot supply one. The sign-in proves more than the message it prints: `login accepted` is written only after the pending state matched, the code was exchanged with the PKCE verifier, and the ID token verified against the live key set — **the subject allow-list included**, which is refused *there* and nowhere earlier, so a real identity is the only thing that can exercise it. The half no success message can show is that the session is **consumed**, and the edge's log shows it as a contrast: before the callback a navigation to `/` answered `303` to `/auth/login`, and afterwards `/` and every screen answered `200` — which means the gate's own verifier accepted the cookie on each request, not that a shell rendered. The refusal path answered `401` with its reason in the log alone, and the API logged no refusal throughout. **What it does not prove** is authorization: a verified identity says nothing yet about roles or areas; no write went outward; and the document-tool half of the read path is exactly as unreachable as it was. The screens have now been served a real session for the first time, which makes a browser console on them the cheap way to confirm or clear the two recorded CSP violations |
| 2026-09-23 | FUP | [FUP-2026-09-23-repo-hygiene.md](FUP-2026-09-23-repo-hygiene.md) | Two settings that had stopped doing anything, and a tracked file `.gitignore` already refused. The `eslint` key in `apps/web/next.config.mjs` is removed — Next 16 deleted the option *and* the lint step it controlled (`ignoreDuringBuilds` had nothing left to suppress), so the key was rejected with a warning on every build while looking like policy. `.cache_ggshield` is untracked with `git rm --cached` (two SHA-256 digests and two test paths — hygiene, not a leak). **The `middleware` → `proxy` warning in the same build is deliberately *not* fixed here**: that rename moves the file carrying every security header and the origin check, and W14 already recorded the failure mode once — middleware silently not loading, build green either way. It gets a check that the policy is still sent before it gets a rename |

## Format

```markdown
# <Workstream> · <date> · <short title>

**Agent/author** · **Duration** · **PR** #n · **Outcome:** complete | partial | blocked

## What was done
## Decisions taken            and why. New ADRs raised, if any
## Surprises                  what differed from the brief or the specs
## Follow-ups                 what the next person should know, with owners
## Specs touched              docs updated because reality diverged
```

## Rules

1. **One entry per run**, even a short or failed one. A blocked run is worth more to the next person
   than silence.
2. **Update this index** in the same commit.
3. **Correct the specs** when reality diverges from them, and say so under *Specs touched*. A spec
   nobody trusts is worse than no spec, because it is cited with confidence.
4. **Record what you did not do** and why. An unexplained gap gets rediscovered the expensive way.
