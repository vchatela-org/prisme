# W12 · 2026-09-19 · Adoption queue and migration

**Agent** claude (Opus 5) · **PR** — · **Outcome:** in progress

> ⚠ **This entry is being written as the run happens**, so that a session that ends mid-way can be
> picked up without reverse-engineering the branch. The *Run state* section below is the handover;
> it is deleted when the run finishes and the entry is rewritten in the normal format.

---

## Run state — read this first if you are picking this up

**Branch:** `ws/w12-adoption`, cut from `main` at `fb9751c`.

### The shape that was decided before any code was written

Candidate discovery is a **scan**, not a live query. `prisme-sync adopt` reads both external tools,
resolves identity, and writes the result into `adoption_candidate` — a mirror of the last scan,
level-triggered, fully replaced each run. Decisions live elsewhere (`entity_link`,
`adoption_ignore`) and are never overwritten by a scan. The queue UI reads the mirror, so opening a
page never touches an external API.

Three tables, one decision each:

| Table | Holds | Written by |
|---|---|---|
| `adoption_candidate` | the last scan's unresolved candidates + proposals | the scan, wholesale |
| `adoption_ignore` | permanent *ignore* decisions — append-only | a human, one at a time |
| `entity_link` (exists, W01) | every adopt/merge decision, with rule and confidence | scan (certain only) or human |

### Checklist

- [ ] 1. Migration `0005_adoption.sql` — `adoption_candidate`, `adoption_ignore`
- [ ] 2. Pure core in `apps/sync/src/adoption/` — normalise, similarity, classify, resolve, queue
- [ ] 3. Coverage/audit report — link coverage, unresolved remainder, would-create count
- [ ] 4. `prisme-sync adopt --plan` wired into `apps/sync/src/main.ts`
- [ ] 5. Store port + SQL implementation
- [ ] 6. API: queue read, adopt, ignore (merge is W05's existing `POST /adoption/decisions`)
- [ ] 7. Web `/adoption` queue screen
- [ ] 8. Adversarial test: adopted corpus → zero `create`, every entity kind
- [ ] 9. Near-miss fixtures that must **not** match
- [ ] 10. Convergence test: ignore survives a re-scan
- [ ] 11. Journal rewritten, `STATUS.md` row, PR green

### What is deliberately not here

No live-data dry run is recorded in this repository, and none can be: the output carries real titles
(`apps/sync/CLAUDE.md`). The definition of done's "real dry run showing `create: 0`" is a human step
against the live instance, and it is recorded as such rather than faked with fixtures.

---

## What was done

*(written at the end)*

## Decisions taken

*(written at the end)*

## Surprises

*(written at the end)*

## Follow-ups

*(written at the end)*

## Specs touched

*(written at the end)*
