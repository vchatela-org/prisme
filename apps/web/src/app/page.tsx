/**
 * Placeholder index route.
 *
 * Every real surface — Focus, Backlog, Areas, Timeline, Objectives, Reviews —
 * is owned by a later workstream and arrives in its own route group. This page
 * exists so the image has something to serve and so `next build` has a route to
 * build; it holds no product content and no fixture data.
 */
export default function Page() {
  return (
    <main>
      <h1>prisme</h1>
      <p>The decision layer between thinking and doing.</p>
    </main>
  );
}
