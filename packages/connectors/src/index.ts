/**
 * Workspace slot. W00 creates the package; W03 fills it.
 *
 * See docs/40-workstreams/W03-connectors.md. Every response that arrives here
 * is untrusted input and is parsed with Zod before anything else sees it.
 */

export {};
