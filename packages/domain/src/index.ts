/**
 * Workspace slot. W00 creates the package; W01 fills it.
 *
 * See docs/40-workstreams/W01-domain-scoring.md and the non-negotiables in
 * ./../CLAUDE.md. The purity rules — no I/O, no ambient clock, no randomness —
 * are enforced by ESLint from the repository root, not by convention.
 */

export {};
